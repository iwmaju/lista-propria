lista = [ ]

while True:
    elemento = input("Digite algo para adicionar à lista (ou 'parar' para terminar):")
    if elemento == "parar":
        break
    lista.append(elemento)

print("Lista completa: ", lista)
